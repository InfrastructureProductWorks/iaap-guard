name: Portfolio Status

on:
  pull_request:
    paths:
      - '.portfolio/status.json'
      - '.github/workflows/portfolio-status.yml'
  push:
    branches: [main]
    paths:
      - '.portfolio/status.json'
      - '.github/workflows/portfolio-status.yml'

permissions:
  contents: read

jobs:
  validate:
    uses: InfrastructureProductWorks/.github/.github/workflows/portfolio-product-status.yml@2fde9dbe5869f9ea65a24a76f6bc32ed7be0518f
    with:
      expected_id: guard
      expected_repository: InfrastructureProductWorks/iaap-guard
      expected_versions_json: '{"product":"1.0.2"}'
